#include "main.h"
/**
 * _islower - returns 1 if is lower case else 1
 *
 * Return: void
 * On error, returnnothing.
 */
int _islower(int c)
{
if (c >= 65 && c <= 90 )
{
return (0);
}
else
{
return (1);
}
}
