#include "main.h"
/**
 * _isalpha - returns 1 if is alpha case else 1
 * @c: The int to check
 * Return: void
 * On error, returnnothing.
 */
int _isupper(int c)
{

if (c >= 48 && c <= 57)
{
return (1);
}
else
{
return (0);
}
}
