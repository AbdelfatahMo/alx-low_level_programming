#include "main.h"
/**
 * mul - returns 1 if is alpha case else 1
 * @a: The int to check
 * @b: The int to check
 * Return: void
 * On error, returnnothing.
 */
int mul(int a, int b)
{
return (a * b);
}
